cmake_minimum_required(VERSION 3.23)

# This CMake file manages the shader files for the ChameleonRT project
# Currently, it doesn't need to do anything as shaders are directly
# included by other components, but it's here to ensure proper file
# organization and future extensibility.
